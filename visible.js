document.addEventListener('DOMContentLoaded', () => {
    const sideImages = document.querySelector('.side-images');
    sideImages.style.display = 'flex'; // Láthatóvá teszi
    sideImages.style.visibility = 'visible'; // Láthatóság beállítása
    sideImages.style.opacity = '1'; // Átlátszatlanság
});